SGT<-function(X,y,X_u,c,d) {
	
	# A_temp<-
	# A<-A_temp + t(A_temp)
	# B<-
	# L<-inv(B) %*% (B-A)
	# D and V eigen(L) #Only need 2-d+1
	# D<-diag(rep(1,d)^2)
	
	# gamma_plus<-
	# gamma_neg<-
	# gamma<-

	# C
	# G=D+c t(V) %*% C %*% V
	# b<-c t(V) %*% C %*% gamma
	# z<-V %*% inv(G-lambda*I) %*% b
	# threshold
}
